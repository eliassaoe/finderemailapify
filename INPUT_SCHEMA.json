{
  "title": "Email Validator Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "emails": {
      "title": "Email Addresses",
      "type": "array",
      "editor": "stringList",
      "description": "List of email addresses to validate (one per line)",
      "example": ["john.doe@example.com", "jane.smith@company.com"],
      "placeholderValue": ["email1@example.com", "email2@company.com"]
    },
    "delayBetweenRequests": {
      "title": "Delay Between Requests (ms)",
      "type": "integer",
      "editor": "number",
      "description": "Delay between each validation request in milliseconds",
      "default": 1000,
      "minimum": 100,
      "maximum": 10000
    }
  },
  "required": ["emails"]
}
